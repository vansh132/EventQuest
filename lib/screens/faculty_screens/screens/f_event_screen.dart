import 'package:flutter/material.dart';

class FacultyEventScreen extends StatelessWidget {
  const FacultyEventScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Events"),
      ),
    );
  }
}
