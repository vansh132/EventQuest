import 'package:flutter/material.dart';

class FacultyGuidelineScreen extends StatelessWidget {
  const FacultyGuidelineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
